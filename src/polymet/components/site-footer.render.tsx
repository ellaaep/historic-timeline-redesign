import { BrowserRouter } from "react-router-dom";
import SiteFooter from "@/polymet/components/site-footer";

export default function SiteFooterRender() {
  return (
    <BrowserRouter>
      <SiteFooter />
    </BrowserRouter>
  );
}
